docker

docker -- version

docker images

// container
docker ps -a   // contener run now
docker container ls -a   // contener run now

// pull
docker pull 
docker pull mysql
docker pull mysql:latest
docker pull mysql:8.0.23
docker pull node:14.8
docker pull node:14.8.0-alpine

// run
docker run --rm --name test1 -p 3306:3306  -e MYSQL_ROOT_PASSWORD=1234 -d mysql

//start 
docker start 203a83de9739 
// stop
docket stop test1


//! delete 
    // delete (container)
docker rm 1160a1e5c894      // (CONTAINER ID )
    // delete (images)
docker image rm hello-world            // (EPOSITORY)
docker image rm hello-world:latest     //(EPOSITORY:TAG)
docker image rm d1165f221234            // (IMAGE ID )

//network
docker network ls

//compose
docker-compose up -d       // run file docker-compose.yml
docker-compose down     

// mysql
docker exec -it test1 bash    // go to mysql shell
cat /etc/os-release         // see OS
which mysql                 // check

mysql -u root -p
show databases;


// phpmyadmin
docker pull phpmyadmin/phpmyadmin
docker run --name myadmin -d --link test1:db -p 8081:80 phpmyadmin/phpmyadmin
docker pull phpmyadmin:5.1



 